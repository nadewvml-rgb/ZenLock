<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />
    <uses-permission android:name="android.permission.USE_FULL_SCREEN_INTENT" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.REORDER_TASKS" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Usage Stats permission for mobile usage tracking -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />

    <!-- Declare that telephony is used, but not strictly required -->
    <uses-feature android:name="android.hardware.telephony" android:required="false" />

    <queries>
        <!-- Allow querying all apps with launcher activities (for whitelist selection) -->
        <intent>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent>

        <!-- Essential system apps -->
        <package android:name="com.android.phone"/>  <!-- Calls -->
        <package android:name="com.google.android.dialer"/>
        <package android:name="com.android.mms"/>  <!-- SMS -->
        <package android:name="com.google.android.apps.messaging"/>
        <package android:name="com.android.deskclock"/>  <!-- Clock -->
        <package android:name="com.google.android.deskclock"/>
        <package android:name="com.sec.android.app.clockpackage"/>  <!-- Samsung Clock -->
    </queries>

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="ZenLock"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:enableOnBackInvokedCallback="true"
        android:supportsRtl="true"
        android:theme="@style/Theme.ZenLock"
        tools:targetApi="31">


        <!-- Main Activity (App Launch) -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.ZenLock">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Permissions Onboarding Activity (First Launch) -->
        <activity
            android:name=".PermissionsOnboardingActivity"
            android:exported="false"
            android:theme="@style/Theme.ZenLock" />

        <!-- Lock Screen Activity -->
        <activity
            android:name=".LockScreenActivity"
            android:taskAffinity=""
            android:excludeFromRecents="true"
            android:theme="@style/Theme.ZenLock"/>

        <!-- Whitelist Activity -->
        <activity
            android:name=".WhitelistActivity"
            android:exported="false"
            android:theme="@style/Theme.ZenLock" />

        <!-- Partner Contact Activity -->
        <activity
            android:name=".PartnerContactActivity"
            android:exported="false"
            android:theme="@style/Theme.ZenLock" />

                <!-- Accessibility Service for App Blocking -->
                <service
                    android:name=".AppBlockerService"
                    android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
                    android:exported="false">
                    <intent-filter>
                        <action android:name="android.accessibilityservice.AccessibilityService" />
                    </intent-filter>
                </service>

                <!-- Lock Screen Service for Background Activity Launch -->
                <service
                    android:name=".LockScreenService"
                    android:enabled="true"
                    android:exported="false"
                    android:foregroundServiceType="specialUse" />

                <!-- Notification Blocker Service -->
                <service
                    android:name=".NotificationBlockerService"
                    android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
                    android:exported="true">
                    <intent-filter>
                        <action android:name="android.service.notification.NotificationListenerService" />
                    </intent-filter>
                </service>

        <!-- Boot Receiver (Runs on Device Restart) -->
        <receiver
            android:name=".BootReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Schedule Trigger Receiver -->
        <receiver
            android:name=".ScheduleTriggerReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- Re-registers alarms after app updates, permission or clock changes -->
        <receiver
            android:name=".ScheduleRefreshReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED" />
                <action android:name="android.intent.action.TIME_SET" />
                <action android:name="android.intent.action.TIMEZONE_CHANGED" />
                <action android:name="android.app.action.SCHEDULE_EXACT_ALARM_PERMISSION_STATE_CHANGED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".ManualStartDelayReceiver"
            android:enabled="true"
            android:exported="false" />

        <receiver
            android:name=".ManualStartCancelReceiver"
            android:enabled="true"
            android:exported="false">
            <intent-filter>
                <action android:name="com.grepguru.zenlock.ACTION_CANCEL_MANUAL_START" />
            </intent-filter>
        </receiver>

        <!-- Pre-Notification Receiver -->
        <receiver
            android:name=".PreNotificationReceiver"
            android:enabled="true"
            android:exported="false" />

        <!-- SMS Status Receivers -->
        <receiver
            android:name=".utils.OTPManager$SmsSentReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.grepguru.zenlock.SMS_SENT_ACTION" />
            </intent-filter>
        </receiver>
        <receiver
            android:name=".utils.OTPManager$SmsDeliveredReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.grepguru.zenlock.SMS_DELIVERED_ACTION" />
            </intent-filter>
        </receiver>


        <!-- Device Admin Receiver — enables appearance in Settings → Security → Device Admin Apps
             for uninstall prevention. User must manually activate it there. -->
        <receiver
            android:name=".ZenLockDeviceAdmin"
            android:permission="android.permission.BIND_DEVICE_ADMIN"
            android:exported="true">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
            </intent-filter>
        </receiver>

    </application>



</manifest>
